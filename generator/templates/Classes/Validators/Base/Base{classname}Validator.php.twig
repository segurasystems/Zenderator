<?php
namespace {{ class.namespace }}\Validators\Base;
use {{ class.namespace }}\Validators\Base\Base{{ class.name }}Validator;

class Base{{ class.name }}Validator extends AbstractValidator
{
    public function getRules() {
        return [
        ];
    }
}